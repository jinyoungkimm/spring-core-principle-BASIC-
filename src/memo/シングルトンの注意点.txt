【シングルトンの注意点】

・特定のクライアントが変更可能なメンバ変数を持ってはいけない
　（読み取り専用のメンバ変数は問題ない）

・Spring Bean（シングルトン）は絶対に stateful に設計してはいけない
　必ず stateless に設計すること

→ やむを得ず stateful な設計が必要な場合は、
　共有メンバ変数ではなく、
　メソッドの引数、ローカル変数、ThreadLocal、セッションなどを使用する