そのような場合には、依存オブジェクトが存在しないときにデフォルトの処理を実行するといった設計が行われることがあります。

しかし、@Autowired は デフォルト設定では依存関係の注入を必須とするため、
Spring コンテナに注入対象となる Bean が存在しない場合、
Spring Boot はアプリケーション起動時にエラーを発生させます。

今回は、@Autowired における 依存関係注入のオプション設定について学習します。

